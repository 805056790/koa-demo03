import React from "react";
import ReactDOM from "react-dom";
import UserList from "./components/UserList";

// 渲染index.html root div.
ReactDOM.render(
    <UserList/>, 
    document.getElementById("root")
);
